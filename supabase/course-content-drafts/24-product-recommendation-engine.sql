insert into public.course_content_draft (course_id, what_you_build, what_you_learn, session, starter_code, test_it_out, troubleshooting, resources, tier, change_note)
values (
  24,
  $wyb$A recommendation pipeline that loads customer purchase history, segments customers by behavior using pandas, loads your product catalog, and asks the AI to generate the top 5 personalized recommendations per customer with reasoning and a confidence score. Two guards run on every reply: one drops any recommended SKU that isn't actually in the catalog, and one drops any SKU the customer already owns -- enforced in code, not just requested in the prompt -- with output ready to push into HubSpot as contact notes.$wyb$,
  $wyl$[
  "Segment customers with simple, explainable rule-based buckets, tested against exact boundary values",
  "Ground a recommendation prompt in a real product catalog, and verify every SKU it returns actually exists there",
  "Enforce a 'never recommend what they already own' rule in code, not just as a prompt instruction",
  "Filter low-confidence suggestions and fall back to real segment best-sellers instead of leaving a customer with nothing",
  "Push structured results into a CRM as notes",
  "Tag a batch with a campaign ID for later A/B conversion analysis"
]$wyl$::jsonb,
  $sess${
  "model": "Google Gemini (gemini-flash-latest) or Claude Haiku 4.5 - your choice, see sdt_ai.py setup",
  "builds": [
    {
      "time": "25 min",
      "steps": [
        {
          "instruction": "Make an empty folder for this project and open it in your code editor (in VS Code: File > Open Folder). Inside it, create `requirements.txt` with this content - it lists every library this project uses:",
          "prompt": "requests\npython-dotenv\npandas\n",
          "verify": "requirements.txt sits directly inside your project folder, not in a subfolder. The next step installs everything it lists."
        },
        {
          "instruction": "Set up Python for this project (once, about 5 minutes). Check your version with `python3 --version` on Mac/Linux or `py --version` on Windows. If it is missing or older than 3.10, install the latest Python from python.org/downloads (on Windows, tick \"Add python.exe to PATH\" on the installer's first screen), then open a new terminal. Now open a terminal in your project folder (in VS Code: Terminal > New Terminal) and run the commands below for your system. They create a virtual environment - a private set of libraries for this project only - and install everything requirements.txt lists.",
          "prompt": "# Mac / Linux\npython3 -m venv .venv\nsource .venv/bin/activate\npython -m pip install -r requirements.txt\n\n# Windows (PowerShell)\npy -m venv .venv\n.venv\\Scripts\\Activate.ps1\npython -m pip install -r requirements.txt\n\n# Windows says \"running scripts is disabled\"? Run this once, then the Activate line again:\n#   Set-ExecutionPolicy -Scope CurrentUser RemoteSigned\n# Ubuntu/Debian says venv or ensurepip is not available? Run: sudo apt install python3-venv",
          "verify": "Your terminal prompt now starts with (.venv), and the install ended with \"Successfully installed\". Each time you open a new terminal for this project, run the activate line again first - while it is active, plain `python` works on every system, and that is the command every step below uses."
        },
        {
          "prompt": "\"\"\"\nSocial Dev Technologies - your connection to the AI.\n\nYou have TWO ways to power this file. Pick ONE and set it in your .env:\n\n  AI_PROVIDER=gemini    -> your own FREE Google Gemini API key\n  AI_PROVIDER=claude    -> your own Claude/Anthropic API key\n\nEvery project in this course calls ask_ai(...) the exact same way no\nmatter which one you pick - this file is the only place that changes.\n\nSETUP - OPTION 1: Gemini (free, no billing needed):\n\n  1. Go to https://aistudio.google.com/apikey and sign in with any Google\n     account. Click \"Create API key\" - free, no card required.\n  2. In the same folder as this file, create a file named exactly: .env\n  3. Put these two lines inside it:\n\n         AI_PROVIDER=gemini\n         GEMINI_API_KEY=paste_your_own_key_here\n\nSETUP - OPTION 2: Claude (if you already have a paid Claude account):\n\n  1. Go to https://console.anthropic.com/settings/keys and create an API\n     key. This needs billing set up on your Anthropic account - it is a\n     separate thing from a claude.ai Pro subscription, and is billed by\n     Anthropic based on what you actually use.\n  2. In your .env file, put these two lines instead:\n\n         AI_PROVIDER=claude\n         ANTHROPIC_API_KEY=paste_your_own_key_here\n\nNever share either key or put it on GitHub - each one is billed to YOUR\naccount. That's it. Every project in this course reuses this same file.\n\"\"\"\n\nimport os\nimport time\n\nimport requests\nfrom dotenv import load_dotenv\n\n# Reads the .env file sitting next to your project and loads your keys.\nload_dotenv()\n\n# \"gemini-flash-latest\" is Google's name for whatever its newest Flash model\n# is, so this file keeps working when Google retires an older model - which\n# it does regularly. You can pin one specific model with GEMINI_MODEL=... in\n# your .env, but if you ever get a \"not available\" error, delete that line.\nGEMINI_MODEL = os.environ.get(\"GEMINI_MODEL\", \"gemini-flash-latest\")\nGEMINI_URL_TEMPLATE = \"https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent\"\n\n# Gemini \"thinks\" before it answers, and that hidden thinking counts toward\n# the same token limit as the answer itself. With a small max_tokens (the\n# 50-300 used for short replies in this course) the thinking could use up\n# the whole limit and the answer would come back empty. So this file turns\n# thinking down to its minimum (these projects don't need it) and gives\n# Gemini this many extra tokens on top of your max_tokens for whatever\n# thinking is left.\nTHINKING_ALLOWANCE = 1024\n\nCLAUDE_MODEL = os.environ.get(\"CLAUDE_MODEL\", \"claude-haiku-4-5\")\nANTHROPIC_URL = \"https://api.anthropic.com/v1/messages\"\nANTHROPIC_VERSION = \"2023-06-01\"\n\n# Both AIs occasionally answer \"overloaded\" during busy periods (Gemini's\n# free tier especially). That's not your code being wrong - retrying a\n# couple of times almost always gets a real answer, so it's built in here\n# rather than left for every project to handle separately.\nMAX_ATTEMPTS = 3\nRETRY_DELAY_SECONDS = 3\n\n\nclass AIError(Exception):\n    \"\"\"Something went wrong talking to the AI. The message explains what.\"\"\"\n\n\ndef _get_provider():\n    \"\"\"\n    Reads AI_PROVIDER from .env. Falls back to guessing from whichever key\n    is actually present, so forgetting to set AI_PROVIDER explicitly\n    doesn't crash a setup that only has one key configured.\n    \"\"\"\n    provider = os.environ.get(\"AI_PROVIDER\", \"\").strip().lower()\n    if provider in (\"gemini\", \"claude\"):\n        return provider\n\n    if os.environ.get(\"GEMINI_API_KEY\"):\n        return \"gemini\"\n    if os.environ.get(\"ANTHROPIC_API_KEY\"):\n        return \"claude\"\n\n    raise AIError(\n        \"No AI provider configured.\\n\"\n        \"Add ONE of these pairs to your .env file:\\n\\n\"\n        \"    AI_PROVIDER=gemini\\n\"\n        \"    GEMINI_API_KEY=paste_your_free_key_here\\n\\n\"\n        \"  ...or...\\n\\n\"\n        \"    AI_PROVIDER=claude\\n\"\n        \"    ANTHROPIC_API_KEY=paste_your_own_key_here\\n\\n\"\n        \"Get a free Gemini key at https://aistudio.google.com/apikey\"\n    )\n\n\ndef _ask_gemini(prompt, system=None, max_tokens=1000):\n    api_key = os.environ.get(\"GEMINI_API_KEY\")\n    if not api_key:\n        raise AIError(\n            \"AI_PROVIDER is set to gemini but no GEMINI_API_KEY was found.\\n\"\n            \"Get a free key at https://aistudio.google.com/apikey and add \"\n            \"it to .env:\\n\"\n            \"    GEMINI_API_KEY=paste_your_own_key_here\"\n        )\n\n    generation_config = {\"maxOutputTokens\": max_tokens + THINKING_ALLOWANCE}\n    # thinkingBudget 0 = \"as little thinking as possible\". Pro models can't\n    # turn thinking down and reject this setting, so it's left off for them.\n    if \"-pro\" not in GEMINI_MODEL:\n        generation_config[\"thinkingConfig\"] = {\"thinkingBudget\": 0}\n\n    payload = {\n        \"contents\": [{\"role\": \"user\", \"parts\": [{\"text\": prompt}]}],\n        \"generationConfig\": generation_config,\n    }\n    if system:\n        payload[\"systemInstruction\"] = {\"parts\": [{\"text\": system}]}\n\n    url = GEMINI_URL_TEMPLATE.format(model=GEMINI_MODEL)\n    # The key goes in a header rather than the URL, so it never shows up in\n    # an error message or traceback you might paste somewhere for help.\n    headers = {\"x-goog-api-key\": api_key}\n\n    last_problem = \"no response\"\n    for attempt in range(MAX_ATTEMPTS):\n        try:\n            response = requests.post(url, headers=headers, json=payload, timeout=90)\n        except requests.RequestException:\n            raise AIError(\n                \"Could not reach Gemini. Check your internet connection and try again.\"\n            ) from None\n\n        if response.status_code in (500, 503):\n            last_problem = f\"Gemini is busy ({response.status_code} - high demand on the free tier)\"\n            if attempt < MAX_ATTEMPTS - 1:\n                time.sleep(RETRY_DELAY_SECONDS)\n            continue\n\n        if response.status_code == 429:\n            raise AIError(\n                \"Gemini says you've hit the free tier's rate limit. Wait a \"\n                \"minute and try again - the free tier only allows a \"\n                \"limited number of requests per minute.\"\n            )\n\n        if response.status_code == 404:\n            if os.environ.get(\"GEMINI_MODEL\"):\n                advice = (\n                    \"Your .env file sets GEMINI_MODEL - delete that line so this \"\n                    \"file uses its default model, then try again.\"\n                )\n            else:\n                advice = \"Check the course page for an updated copy of sdt_ai.py.\"\n            raise AIError(\n                f'Gemini says the model \"{GEMINI_MODEL}\" isn\\'t available to your '\n                f\"API key - Google retires older models.\\n{advice}\\n\"\n                f\"Details: {response.text[:300]}\"\n            )\n\n        if response.status_code == 400:\n            # If a future Gemini model ever refuses the thinking setting\n            # above, ask again without it instead of failing.\n            if \"thinkingConfig\" in generation_config and \"thinking\" in response.text.lower():\n                del generation_config[\"thinkingConfig\"]\n                last_problem = \"Gemini rejected the thinking setting\"\n                continue\n            raise AIError(\n                f\"Gemini rejected the request - double check your GEMINI_API_KEY \"\n                f\"is correct: {response.text[:300]}\"\n            )\n\n        if response.status_code != 200:\n            raise AIError(\n                f\"Gemini returned an error (status {response.status_code}): {response.text[:300]}\"\n            )\n\n        data = response.json()\n        candidates = data.get(\"candidates\") or []\n        if not candidates:\n            block_reason = (data.get(\"promptFeedback\") or {}).get(\"blockReason\")\n            if block_reason:\n                raise AIError(f\"Gemini refused the prompt ({block_reason}). Try rephrasing it.\")\n            raise AIError(f\"Unexpected reply from Gemini: {data}\")\n        candidate = candidates[0]\n        finish_reason = candidate.get(\"finishReason\")\n\n        # A response can come back with no text because Gemini's safety\n        # filters blocked it (finishReason == \"SAFETY\"), not because of a\n        # network problem - so this is not retried like the cases above.\n        if finish_reason == \"SAFETY\":\n            raise AIError(\"Gemini declined to answer (safety filter). Try rephrasing your prompt.\")\n\n        # An answer can arrive split across several parts, so join them all.\n        # Any part marked \"thought\" is Gemini's thinking, not its answer.\n        parts = (candidate.get(\"content\") or {}).get(\"parts\") or []\n        text = \"\".join(part.get(\"text\", \"\") for part in parts if not part.get(\"thought\"))\n        if not text:\n            if finish_reason == \"MAX_TOKENS\":\n                raise AIError(\n                    \"Gemini ran out of room before it wrote any answer - its thinking \"\n                    f\"used up all {generation_config['maxOutputTokens']} tokens it was \"\n                    \"allowed. Raise max_tokens in this ask_ai(...) call (try \"\n                    f\"max_tokens={max(max_tokens * 2, 4000)}) and run it again.\"\n                )\n            raise AIError(\n                f\"Gemini sent back an empty answer (finishReason: {finish_reason}). \"\n                \"Try again, or rephrase your prompt.\"\n            )\n\n        usage = data.get(\"usageMetadata\") or {}\n        return {\n            \"text\": text,\n            \"input_tokens\": usage.get(\"promptTokenCount\"),\n            \"output_tokens\": usage.get(\"candidatesTokenCount\"),\n        }\n\n    raise AIError(\n        f\"Gemini was unavailable after {MAX_ATTEMPTS} tries ({last_problem}). \"\n        \"This happens sometimes on the free tier during high demand - wait \"\n        \"a minute and try again.\"\n    )\n\n\ndef _ask_claude(prompt, system=None, max_tokens=1000):\n    api_key = os.environ.get(\"ANTHROPIC_API_KEY\")\n    if not api_key:\n        raise AIError(\n            \"AI_PROVIDER is set to claude but no ANTHROPIC_API_KEY was found.\\n\"\n            \"Create one at https://console.anthropic.com/settings/keys and \"\n            \"add it to .env:\\n\"\n            \"    ANTHROPIC_API_KEY=paste_your_own_key_here\"\n        )\n\n    payload = {\n        \"model\": CLAUDE_MODEL,\n        \"max_tokens\": max_tokens,\n        \"messages\": [{\"role\": \"user\", \"content\": prompt}],\n    }\n    if system:\n        payload[\"system\"] = system\n\n    last_problem = \"no response\"\n    for attempt in range(MAX_ATTEMPTS):\n        try:\n            response = requests.post(\n                ANTHROPIC_URL,\n                headers={\n                    \"x-api-key\": api_key,\n                    \"anthropic-version\": ANTHROPIC_VERSION,\n                    \"Content-Type\": \"application/json\",\n                },\n                json=payload,\n                timeout=90,\n            )\n        except requests.RequestException:\n            raise AIError(\n                \"Could not reach Claude. Check your internet connection and try again.\"\n            ) from None\n\n        # 529 is Anthropic's \"overloaded\" - like Gemini's 503, a retry\n        # usually works.\n        if response.status_code in (500, 503, 529):\n            last_problem = f\"Claude is busy ({response.status_code} - overloaded)\"\n            if attempt < MAX_ATTEMPTS - 1:\n                time.sleep(RETRY_DELAY_SECONDS)\n            continue\n\n        try:\n            data = response.json()\n        except ValueError:\n            raise AIError(f\"Unexpected reply from Claude (status {response.status_code}).\") from None\n\n        if response.status_code == 401:\n            raise AIError(\n                \"Claude rejected your ANTHROPIC_API_KEY - double check it was copied correctly.\"\n            )\n\n        if response.status_code == 429:\n            raise AIError(\"Claude says you've hit your rate or usage limit. Wait a moment and try again.\")\n\n        if response.status_code == 404:\n            if os.environ.get(\"CLAUDE_MODEL\"):\n                advice = (\n                    \"Your .env file sets CLAUDE_MODEL - delete that line so this \"\n                    \"file uses its default model, then try again.\"\n                )\n            else:\n                advice = \"Check the course page for an updated copy of sdt_ai.py.\"\n            raise AIError(\n                f'Claude says the model \"{CLAUDE_MODEL}\" isn\\'t available to your '\n                f\"API key.\\n{advice}\"\n            )\n\n        if \"error\" in data or response.status_code != 200:\n            error = data.get(\"error\") or {}\n            message = error.get(\"message\") if isinstance(error, dict) else str(error)\n            raise AIError(\n                f\"Claude returned an error (status {response.status_code}): \"\n                f\"{message or str(data)[:300]}\"\n            )\n\n        text = \"\".join(\n            block.get(\"text\", \"\") for block in data.get(\"content\", []) if block.get(\"type\") == \"text\"\n        )\n        if not text:\n            raise AIError(\n                f\"Claude sent back an empty answer (stop_reason: {data.get('stop_reason')}). \"\n                \"Try again, or rephrase your prompt.\"\n            )\n\n        usage = data.get(\"usage\") or {}\n        return {\n            \"text\": text,\n            \"input_tokens\": usage.get(\"input_tokens\"),\n            \"output_tokens\": usage.get(\"output_tokens\"),\n        }\n\n    raise AIError(\n        f\"Claude was unavailable after {MAX_ATTEMPTS} tries ({last_problem}). \"\n        \"Wait a minute and try again.\"\n    )\n\n\ndef ask_ai_detailed(prompt, system=None, max_tokens=1000, project=None):\n    \"\"\"\n    Same as ask_ai, but returns the whole reply as a dict.\n\n        result = ask_ai_detailed(\"Summarise this\", max_tokens=200)\n        print(result[\"text\"])\n    \"\"\"\n    provider = _get_provider()\n    if provider == \"gemini\":\n        return _ask_gemini(prompt, system=system, max_tokens=max_tokens)\n    return _ask_claude(prompt, system=system, max_tokens=max_tokens)\n\n\ndef ask_ai(prompt, system=None, max_tokens=1000, project=None):\n    \"\"\"\n    Send a question to the AI and get the answer back as text.\n\n        answer = ask_ai(\"Write a haiku about Lagos traffic\")\n        print(answer)\n\n    prompt      - what you want the AI to do. This is the important part.\n    system      - optional. Sets the AI's role, e.g. \"You are a careful editor.\"\n    max_tokens  - roughly how long the answer may be. 1000 is plenty for most.\n    project     - unused, kept so call sites read the same across every\n                  project regardless of which AI is behind this file.\n\n    Works identically whichever provider your .env is set to - every\n    project in this course calls this one function and never needs to\n    know which AI is actually answering.\n\n    Returns the AI's answer as a plain string.\n    Raises AIError with a readable message if something is wrong.\n    \"\"\"\n    return ask_ai_detailed(prompt, system=system, max_tokens=max_tokens, project=project)[\"text\"]\n\n\nif __name__ == \"__main__\":\n    # Running this file directly checks that your setup works.\n    print(\"Testing your AI connection...\\n\")\n    try:\n        provider = _get_provider()\n        model = GEMINI_MODEL if provider == \"gemini\" else CLAUDE_MODEL\n        result = ask_ai_detailed(\"Say hello in exactly five words.\", max_tokens=50)\n        print(f\"Provider: {provider} ({model})\")\n        print(\"The AI said:\", result[\"text\"])\n        print(\"\\nYour setup works. You are ready to build.\")\n    except AIError as problem:\n        print(\"Setup problem:\\n\")\n        print(problem)\n",
          "verify": "Don't run it yet - it needs the .env file from the next step first.",
          "instruction": "Create `sdt_ai.py` in your project folder with this code. You'll reuse this exact file, unmodified, in every future project in this course:"
        },
        {
          "prompt": "# OPTION 1: your own free Gemini key (recommended - no billing needed)\n# Get one free at https://aistudio.google.com/apikey - no credit card needed\nAI_PROVIDER=gemini\nGEMINI_API_KEY=paste_your_own_key_here\n\n# OPTION 2: your own Claude/Anthropic key (comment OPTION 1 out above,\n# uncomment these two lines instead - needs billing set up on your own\n# Anthropic account)\n# Get one at https://console.anthropic.com/settings/keys\n# AI_PROVIDER=claude\n# ANTHROPIC_API_KEY=paste_your_own_key_here\n",
          "verify": "Run `python sdt_ai.py` - it should print which provider you're using, then `The AI said: ...`, and end with `Your setup works. You are ready to build.` If you see `No AI provider configured` instead, check that .env sits next to sdt_ai.py and that AI_PROVIDER plus its matching key are both uncommented.",
          "instruction": "Create `.env` in the same folder with this content, then replace the placeholders with your real keys:"
        },
        {
          "prompt": "\"\"\"\nStep 1 of the Product Recommendation Engine (part 1) - reads customer\npurchase history from a CSV.\n\"\"\"\n\nimport pandas as pd\n\n\ndef split_purchases(series):\n    \"\"\"\n    Pure function - turns a Series of comma-separated SKU strings into\n    a Series of lists, tested below without a real CSV file.\n\n        split_purchases(pd.Series([\"SKU-1,SKU-2\", None]))\n        -> [[\"SKU-1\", \"SKU-2\"], []]\n    \"\"\"\n    return series.fillna(\"\").apply(lambda s: [sku.strip() for sku in s.split(\",\") if sku.strip()])\n\n\ndef load_customers(path=\"customers.csv\"):\n    \"\"\"\n    Reads path as a CSV and returns a DataFrame with past_purchases\n    turned from a comma-separated string into a real Python list.\n\n        df = load_customers(\"customers.csv\")\n\n    A missing past_purchases value becomes an empty list, not NaN or a\n    crash - a brand-new customer with no purchase history yet is a\n    completely normal row, not a broken one.\n    \"\"\"\n    df = pd.read_csv(path)\n    df[\"past_purchases\"] = split_purchases(df[\"past_purchases\"])\n    return df\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check split_purchases() works - a\n    # real pandas Series, no CSV file needed:  python load.py\n    raw = pd.Series([\"SKU-1,SKU-2\", \"\", None, \"SKU-3\"])\n    result = split_purchases(raw)\n\n    assert result[0] == [\"SKU-1\", \"SKU-2\"]\n    print(\"OK - comma-separated string correctly split:\", result[0])\n\n    assert result[1] == []\n    assert result[2] == []\n    print(\"OK - both an empty string and a missing (None) value correctly became [], not NaN or a crash\")\n\n    assert result[3] == [\"SKU-3\"]\n    print(\"OK - a single SKU with no comma correctly became a one-item list:\", result[3])\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python load.py`. You should see a comma-separated string correctly split, both an empty string and a missing value correctly become an empty list, and a single SKU with no comma correctly become a one-item list - ending with `All checks passed.`",
          "instruction": "Create `load.py` with this code:"
        },
        {
          "prompt": "\"\"\"\nStep 1 of the Product Recommendation Engine (part 2) - buckets\ncustomers into simple, explainable segments.\n\"\"\"\n\n\ndef size_segment(company_size):\n    \"\"\"Pure function - tested below with real boundary values.\"\"\"\n    if company_size < 50:\n        return \"SMB\"\n    if company_size < 500:\n        return \"Mid-Market\"\n    return \"Enterprise\"\n\n\ndef engagement_segment(past_purchases):\n    \"\"\"Pure function - tested below with real boundary values.\"\"\"\n    count = len(past_purchases)\n    if count <= 1:\n        return \"New\"\n    if count <= 4:\n        return \"Active\"\n    return \"Power User\"\n\n\ndef segment_customers(df):\n    \"\"\"\n    Adds size_segment and engagement_segment columns to df.\n\n        segment_customers(df)\n\n    If purchase history lives in a separate orders table in your real\n    system, pivot it into a comma-separated SKU list per customer\n    (matching load.py's expected shape) before calling this.\n    \"\"\"\n    df = df.copy()\n    df[\"size_segment\"] = df[\"company_size\"].apply(size_segment)\n    df[\"engagement_segment\"] = df[\"past_purchases\"].apply(engagement_segment)\n    return df\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check the segmentation boundaries are\n    # exactly right - no CSV file needed:  python segment.py\n    print(\"Checking size_segment() boundaries...\")\n    assert size_segment(49) == \"SMB\"\n    assert size_segment(50) == \"Mid-Market\"  # the boundary itself belongs to the NEXT tier\n    assert size_segment(499) == \"Mid-Market\"\n    assert size_segment(500) == \"Enterprise\"\n    print(\"  OK - 49/50/499/500 all landed in the correct tier\")\n\n    print(\"\\nChecking engagement_segment() boundaries...\")\n    assert engagement_segment([]) == \"New\"\n    assert engagement_segment([\"a\"]) == \"New\"\n    assert engagement_segment([\"a\", \"b\"]) == \"Active\"\n    assert engagement_segment([\"a\", \"b\", \"c\", \"d\"]) == \"Active\"\n    assert engagement_segment([\"a\", \"b\", \"c\", \"d\", \"e\"]) == \"Power User\"\n    print(\"  OK - 0/1/2/4/5 purchases all landed in the correct tier\")\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python segment.py`. You should see company sizes 49/50/499/500 all land in the correct SMB/Mid-Market/Enterprise tier, and purchase counts 0/1/2/4/5 all land in the correct New/Active/Power User tier - ending with `All checks passed.`",
          "instruction": "Create `segment.py` with this code:"
        },
        {
          "prompt": "python -c \"from load import load_customers; from segment import segment_customers; df = segment_customers(load_customers()); print(df[['customer_id', 'size_segment', 'engagement_segment']])\"",
          "verify": "Confirm every customer has a sensible segment - spot-check a couple you know well against the numbers in your CSV.",
          "instruction": "Now run both on your real customer CSV:"
        }
      ],
      "title": "Load customer data and build segments",
      "number": 1,
      "goFurther": "🛠️🛠️ If you have thousands of customers, try k-means clustering on purchase frequency and order value instead of the rule-based buckets.",
      "description": "Read customer purchase history and group customers into simple, explainable segments."
    },
    {
      "time": "40 min",
      "steps": [
        {
          "prompt": "\"\"\"\nStep 2 of the Product Recommendation Engine (part 1) - loads the\nproduct catalog every recommendation is grounded against.\n\nCreate your own catalog.json with your real products in this shape:\n\n[\n  {\"sku\": \"BASIC-1\", \"name\": \"Starter Plan\", \"description\": \"Core features for small teams\", \"price_tier\": \"low\"},\n  {\"sku\": \"PRO-1\", \"name\": \"Analytics Pro\", \"description\": \"Advanced dashboards and reporting\", \"price_tier\": \"mid\"},\n  {\"sku\": \"PRO-2\", \"name\": \"Automation Suite\", \"description\": \"Workflow automation tools\", \"price_tier\": \"mid\"},\n  {\"sku\": \"ENT-1\", \"name\": \"Enterprise SSO\", \"description\": \"Single sign-on and audit logs\", \"price_tier\": \"high\"},\n  {\"sku\": \"ENT-2\", \"name\": \"Dedicated Support\", \"description\": \"24/7 priority support line\", \"price_tier\": \"high\"},\n  {\"sku\": \"ADDON-1\", \"name\": \"API Access\", \"description\": \"Programmatic access to your data\", \"price_tier\": \"low\"}\n]\n\"\"\"\n\nimport json\n\n\ndef load_catalog(path=\"catalog.json\"):\n    \"\"\"Reads path as a JSON file and returns the list of product dicts.\"\"\"\n    with open(path, encoding=\"utf-8\") as f:\n        return json.load(f)\n\n\ndef valid_skus(catalog):\n    \"\"\"Pure function - the set of every real SKU in the catalog, used to catch a hallucinated recommendation.\"\"\"\n    return {product[\"sku\"] for product in catalog}\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check valid_skus() works - a real\n    # catalog list in memory, no JSON file needed:  python catalog.py\n    sample_catalog = [\n        {\"sku\": \"BASIC-1\", \"name\": \"Starter Plan\"},\n        {\"sku\": \"PRO-1\", \"name\": \"Analytics Pro\"},\n    ]\n    skus = valid_skus(sample_catalog)\n    assert skus == {\"BASIC-1\", \"PRO-1\"}\n    print(\"Extracted SKUs:\", skus)\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python catalog.py`. You should see the two sample SKUs correctly extracted, ending with `All checks passed.`",
          "instruction": "Create `catalog.json` with 6-8 of your real products (see the sample shape in catalog.py's docstring below), then create `catalog.py` with this code:"
        },
        {
          "prompt": "\"\"\"\nStep 2 of the Product Recommendation Engine (part 2).\n\nAsks the AI to recommend the top 5 products a customer is likely to buy\nnext. Two safety guards run on every reply: one drops any recommended\nSKU that isn't actually in the catalog (a hallucinated product), and\none drops any recommended SKU the customer already owns - enforced in\ncode, not just requested in the prompt, since \"exclude what they own\"\nis exactly the kind of instruction a model occasionally still misses.\n\"\"\"\n\nimport json\n\nfrom catalog import valid_skus\nfrom sdt_ai import ask_ai\n\n\ndef build_prompt(customer, catalog):\n    return f\"\"\"Customer profile:\nIndustry: {customer.get('industry', 'unknown')}\nCompany size segment: {customer.get('size_segment', 'unknown')}\nEngagement segment: {customer.get('engagement_segment', 'unknown')}\nAlready owns (past purchases): {customer.get('past_purchases', [])}\n\nProduct catalog:\n{json.dumps(catalog)}\n\nRecommend the top 5 products this customer is most likely to buy next,\nEXCLUDING any product they already own. For each, give a confidence\nscore (1-10, and vary it meaningfully based on how strong the fit is -\ndo not give every product the same score) and a one-sentence reason.\n\nReturn ONLY this JSON shape, no markdown code fence, no explanation:\n{{\"recommendations\": [{{\"sku\": \"...\", \"confidence\": 0, \"reason\": \"...\"}}]}}\"\"\"\n\n\ndef parse_recommendations_reply(reply):\n    \"\"\"Defensive JSON parsing, same pattern used throughout this course. Returns None on real garbage.\"\"\"\n    cleaned = reply.strip()\n    if cleaned.startswith(\"```\"):\n        lines = cleaned.splitlines()[1:]\n        if lines and lines[-1].strip() == \"```\":\n            lines = lines[:-1]\n        cleaned = \"\\n\".join(lines)\n\n    try:\n        data = json.loads(cleaned)\n    except json.JSONDecodeError:\n        return None\n    return data.get(\"recommendations\", [])\n\n\ndef validate_recommendations(recommendations, catalog, past_purchases):\n    \"\"\"\n    Drops any recommendation whose SKU isn't actually in the catalog\n    (a hallucinated product), and any SKU the customer already owns -\n    enforced here in code, not left to the prompt instruction alone.\n\n        validate_recommendations(recs, catalog, [\"BASIC-1\"])\n    \"\"\"\n    real_skus = valid_skus(catalog)\n    owned = set(past_purchases)\n\n    kept = []\n    for rec in recommendations:\n        sku = rec.get(\"sku\")\n        if sku not in real_skus:\n            print(f\"  Dropped recommendation for '{sku}' - not a real SKU in the catalog.\")\n            continue\n        if sku in owned:\n            print(f\"  Dropped recommendation for '{sku}' - customer already owns it.\")\n            continue\n        kept.append(rec)\n\n    return kept\n\n\ndef recommend_for_customer(customer, catalog):\n    \"\"\"\n    The main function. Give it one customer row (as a dict) and the\n    catalog, get back a validated list of up to 5 recommendations.\n    \"\"\"\n    prompt = build_prompt(customer, catalog)\n    reply = ask_ai(prompt, max_tokens=600, project=\"product-recommendation-engine\")\n\n    recommendations = parse_recommendations_reply(reply)\n    if recommendations is None:\n        raise ValueError(\"The AI's reply wasn't valid JSON.\\nRun it again - this usually fixes itself.\")\n\n    return validate_recommendations(recommendations, catalog, customer.get(\"past_purchases\", []))\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check parsing and both guards work -\n    # no AI call, no API usage:  python recommend.py\n    catalog = [\n        {\"sku\": \"BASIC-1\", \"name\": \"Starter Plan\"},\n        {\"sku\": \"PRO-1\", \"name\": \"Analytics Pro\"},\n        {\"sku\": \"PRO-2\", \"name\": \"Automation Suite\"},\n    ]\n\n    print(\"Checking a hallucinated SKU (not in the catalog) gets dropped...\")\n    recs = [\n        {\"sku\": \"PRO-1\", \"confidence\": 8, \"reason\": \"Good fit\"},\n        {\"sku\": \"FAKE-999\", \"confidence\": 9, \"reason\": \"This SKU doesn't exist\"},\n    ]\n    result = validate_recommendations(recs, catalog, past_purchases=[])\n    assert len(result) == 1\n    assert result[0][\"sku\"] == \"PRO-1\"\n    print(\"  OK - the real SKU was kept, the hallucinated one was dropped\")\n\n    print(\"\\nChecking an already-owned SKU gets dropped even though it's a real product...\")\n    recs = [\n        {\"sku\": \"BASIC-1\", \"confidence\": 9, \"reason\": \"Already owned - should be filtered\"},\n        {\"sku\": \"PRO-2\", \"confidence\": 7, \"reason\": \"A genuinely new recommendation\"},\n    ]\n    result = validate_recommendations(recs, catalog, past_purchases=[\"BASIC-1\"])\n    assert len(result) == 1\n    assert result[0][\"sku\"] == \"PRO-2\"\n    print(\"  OK - the owned product was dropped even though it's real, the new one was kept\")\n\n    print(\"\\nChecking parse_recommendations_reply() strips a ```json code fence...\")\n    fenced = '```json\\n{\"recommendations\": [{\"sku\": \"PRO-1\", \"confidence\": 8, \"reason\": \"x\"}]}\\n```'\n    assert len(parse_recommendations_reply(fenced)) == 1\n    print(\"  OK\")\n\n    print(\"\\nChecking a non-JSON reply returns None instead of crashing...\")\n    assert parse_recommendations_reply(\"Sorry, I can't recommend anything.\") is None\n    print(\"  OK\")\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python recommend.py` (no AI call, no API usage - this only tests parsing and both guards against fake data). You should see a hallucinated SKU correctly dropped while a real one is kept, an already-owned SKU correctly dropped even though it's a real product, a ```json-fenced reply correctly parsed, and a non-JSON reply correctly returning None - ending with `All checks passed.`",
          "instruction": "Create `recommend.py` with this code:"
        },
        {
          "prompt": "python -c \"from load import load_customers; from segment import segment_customers; from catalog import load_catalog; from recommend import recommend_for_customer; df = segment_customers(load_customers()); customer = df.iloc[0].to_dict(); catalog = load_catalog(); import json; print(json.dumps(recommend_for_customer(customer, catalog), indent=2))\"",
          "verify": "You should get back up to 5 recommendations, none matching SKUs already in that customer's past_purchases and none for a SKU that isn't in catalog.json, each with a confidence score and reason.",
          "instruction": "Now run it on one real customer row:"
        }
      ],
      "title": "Build the catalog and recommender with hallucination guards",
      "number": 2,
      "goFurther": "🛠️ Add a second pass that explains, in one paragraph, the overall theme of this customer's recommendations (e.g. 'upsell toward analytics tier').",
      "description": "Load your product catalog and ask the AI to recommend the top 5 products per customer. Two guards run on every reply here: one drops any SKU that isn't actually in your catalog, and one drops any SKU the customer already owns."
    },
    {
      "time": "30 min",
      "steps": [
        {
          "prompt": "\"\"\"\nStep 3 of the Product Recommendation Engine (part 1) - drops low-\nconfidence suggestions before they reach a real customer or CRM record.\n\"\"\"\n\n\ndef filter_recommendations(recommendations, min_confidence=6):\n    \"\"\"\n    Returns only recommendations with confidence >= min_confidence.\n\n        filter_recommendations(recs, min_confidence=6)\n\n    Pure function - tested below without any AI call.\n    \"\"\"\n    return [r for r in recommendations if r.get(\"confidence\", 0) >= min_confidence]\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check the filter works - no AI call,\n    # no API usage:  python filter.py\n    recs = [\n        {\"sku\": \"A\", \"confidence\": 9},\n        {\"sku\": \"B\", \"confidence\": 5},\n        {\"sku\": \"C\", \"confidence\": 6},\n    ]\n    result = filter_recommendations(recs, min_confidence=6)\n    assert {r[\"sku\"] for r in result} == {\"A\", \"C\"}\n    print(\"Kept:\", [r[\"sku\"] for r in result], \"- confidence-5 recommendation correctly dropped, confidence-6 kept (boundary is inclusive)\")\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python filter.py`. You should see a confidence-5 recommendation correctly dropped while a confidence-6 one is kept (the boundary is inclusive), ending with `All checks passed.`",
          "instruction": "Create `filter.py` with this code:"
        },
        {
          "prompt": "\"\"\"\nStep 3 of the Product Recommendation Engine (part 2) - processes the\nentire customer base into recommendations.csv, falling back to a\nsegment's best-sellers when a customer has zero qualifying\nrecommendations above the confidence threshold.\n\"\"\"\n\nimport pandas as pd\n\nfrom filter import filter_recommendations\nfrom recommend import recommend_for_customer\nfrom sdt_ai import AIError\n\n\ndef compute_bestsellers(df, top_n=3):\n    \"\"\"\n    Pure function - for each size_segment, returns the top_n most\n    frequently purchased SKUs across all customers in that segment.\n\n        compute_bestsellers(df)\n        -> {\"SMB\": [\"SKU-1\", \"SKU-2\", \"SKU-3\"], \"Mid-Market\": [...], ...}\n\n    Used as a fallback in run_batch() below - a customer with zero\n    recommendations above the confidence threshold still gets something\n    concrete to act on, grounded in real purchase data rather than a\n    guess.\n    \"\"\"\n    bestsellers = {}\n    for segment, group in df.groupby(\"size_segment\"):\n        all_skus = [sku for purchases in group[\"past_purchases\"] for sku in purchases]\n        counts = pd.Series(all_skus).value_counts()\n        bestsellers[segment] = counts.head(top_n).index.tolist()\n    return bestsellers\n\n\ndef run_batch(customers_df, catalog, min_confidence=6, output=\"recommendations.csv\"):\n    \"\"\"\n    The main function. Loops over every customer, recommends, filters\n    by confidence, falls back to that segment's best-sellers if nothing\n    qualifies, and writes recommendations.csv.\n    \"\"\"\n    bestsellers = compute_bestsellers(customers_df)\n    rows = []\n\n    for _, customer in customers_df.iterrows():\n        customer_dict = customer.to_dict()\n\n        try:\n            recs = recommend_for_customer(customer_dict, catalog)\n        except (AIError, ValueError) as problem:\n            print(f\"  Skipped {customer_dict.get('customer_id')}: {problem}\")\n            continue\n\n        qualifying = filter_recommendations(recs, min_confidence=min_confidence)\n\n        if not qualifying:\n            fallback_skus = bestsellers.get(customer_dict.get(\"size_segment\"), [])\n            qualifying = [\n                {\n                    \"sku\": sku, \"confidence\": min_confidence,\n                    \"reason\": \"Segment best-seller fallback - no AI recommendation met the confidence threshold\",\n                }\n                for sku in fallback_skus\n            ]\n\n        for rec in qualifying:\n            rows.append({\"customer_id\": customer_dict.get(\"customer_id\"), **rec})\n\n    result_df = pd.DataFrame(rows)\n    result_df.to_csv(output, index=False)\n    return output\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check compute_bestsellers() works - a\n    # real pandas DataFrame built in memory, no AI call needed:\n    #     python batch.py\n    df = pd.DataFrame({\n        \"size_segment\": [\"SMB\", \"SMB\", \"SMB\", \"Enterprise\"],\n        \"past_purchases\": [[\"A\", \"B\"], [\"A\", \"C\"], [\"A\"], [\"Z\"]],\n    })\n    result = compute_bestsellers(df, top_n=2)\n    assert result[\"SMB\"][0] == \"A\"  # \"A\" appears 3 times, the clear top seller\n    print(\"SMB bestsellers:\", result[\"SMB\"])\n    assert result[\"Enterprise\"] == [\"Z\"]\n    print(\"Enterprise bestsellers:\", result[\"Enterprise\"])\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python batch.py` (no AI call - this only tests compute_bestsellers() against a real DataFrame). You should see the SMB segment's most-purchased SKU correctly identified as the top bestseller, and the Enterprise segment's single SKU correctly returned, ending with `All checks passed.`",
          "instruction": "Create `batch.py` with this code:"
        },
        {
          "prompt": "python -c \"from load import load_customers; from segment import segment_customers; from catalog import load_catalog; from batch import run_batch; df = segment_customers(load_customers()); print(run_batch(df, load_catalog()))\"",
          "verify": "recommendations.csv should be created with one row per (customer_id, recommended sku), and every confidence score should be >= 6 - either a real AI recommendation or a best-seller fallback row.",
          "instruction": "Now run the real batch on your full customer DataFrame:"
        }
      ],
      "title": "Filter by confidence and run an overnight batch",
      "number": 3,
      "goFurther": "🛠️🛠️ For thousands of customers, look into your provider's batch API - Anthropic's Message Batches API (linked below) or Gemini's Batch Mode - both cut cost roughly in half for exactly this kind of large, non-urgent batch job.",
      "description": "Drop low-confidence suggestions and process your entire customer list into a single CSV, with a real fallback for anyone who ends up with nothing."
    },
    {
      "time": "30 min",
      "steps": [
        {
          "prompt": "\"\"\"\nStep 4 of the Product Recommendation Engine (part 1) - writes the top\nrecommendation onto each customer's CRM record as a note.\n\"\"\"\n\nimport requests\n\n\ndef build_note_body(recommendation):\n    \"\"\"\n    Pure function - the exact note text, tested below without a real\n    HubSpot account.\n\n        build_note_body({\"sku\": \"PRO-1\", \"confidence\": 8, \"reason\": \"Fits their usage pattern\"})\n    \"\"\"\n    return f\"AI Recommendation: {recommendation['sku']} (confidence {recommendation['confidence']}/10) - {recommendation['reason']}\"\n\n\ndef push_recommendation_note(token, contact_id, recommendation):\n    \"\"\"\n    Creates a HubSpot note associated with contact_id.\n\n    The association type ID for \"note to contact\" (202 in most\n    accounts) should be confirmed in your own HubSpot API docs -\n    HubSpot's default association type IDs occasionally differ by\n    account configuration.\n    \"\"\"\n    note_body = build_note_body(recommendation)\n\n    response = requests.post(\n        \"https://api.hubapi.com/crm/v3/objects/notes\",\n        headers={\"Authorization\": f\"Bearer {token}\", \"Content-Type\": \"application/json\"},\n        json={\n            \"properties\": {\"hs_note_body\": note_body, \"hs_timestamp\": \"now\"},\n            \"associations\": [{\n                \"to\": {\"id\": contact_id},\n                \"types\": [{\"associationCategory\": \"HUBSPOT_DEFINED\", \"associationTypeId\": 202}],\n            }],\n        },\n    )\n    response.raise_for_status()\n    return response.json()\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check the note text is built\n    # correctly - no HubSpot account or network call needed:\n    #     python crm_push.py\n    note = build_note_body({\"sku\": \"PRO-1\", \"confidence\": 8, \"reason\": \"Fits their usage pattern\"})\n    assert note == \"AI Recommendation: PRO-1 (confidence 8/10) - Fits their usage pattern\"\n    print(\"Built note:\", note)\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python crm_push.py`. You should see the note text built exactly as expected, ending with `All checks passed.`",
          "instruction": "Create `crm_push.py` with this code:"
        },
        {
          "prompt": "\"\"\"\nStep 4 of the Product Recommendation Engine (part 2) - tags a batch of\nrecommendations with a campaign ID so conversion can be measured later.\n\"\"\"\n\nimport uuid\n\n\ndef tag_recommendations(rows, campaign_id=None):\n    \"\"\"\n    Adds a campaign_id to every row (a list of dicts), generating a\n    short random one if none is given.\n\n        tag_recommendations(rows, campaign_id=\"spring-upsell\")\n\n    recommendations.csv can later be joined with an orders.csv on\n    customer_id + sku + date to compute conversion rate for recommended\n    vs. non-recommended purchases.\n    \"\"\"\n    campaign_id = campaign_id or str(uuid.uuid4())[:8]\n    return [{**row, \"campaign_id\": campaign_id} for row in rows]\n\n\nif __name__ == \"__main__\":\n    # Run this file on its own to check tagging works - no AI call, no\n    # API usage:  python ab_test.py\n    rows = [{\"customer_id\": \"1\", \"sku\": \"A\"}, {\"customer_id\": \"2\", \"sku\": \"B\"}]\n\n    print(\"Checking an explicit campaign_id is applied to every row...\")\n    tagged = tag_recommendations(rows, campaign_id=\"spring-upsell\")\n    assert all(row[\"campaign_id\"] == \"spring-upsell\" for row in tagged)\n    print(\"  OK -\", tagged)\n\n    print(\"\\nChecking an auto-generated campaign_id is the same across all rows...\")\n    tagged = tag_recommendations(rows)\n    campaign_ids = {row[\"campaign_id\"] for row in tagged}\n    assert len(campaign_ids) == 1\n    assert len(list(campaign_ids)[0]) == 8\n    print(\"  OK - single 8-character campaign_id generated and applied to every row:\", campaign_ids)\n\n    print(\"\\nAll checks passed.\")\n",
          "verify": "Run `python ab_test.py`. You should see an explicit campaign_id applied to every row, and an auto-generated one that's identical across all rows in the same batch, ending with `All checks passed.`",
          "instruction": "Create `ab_test.py` with this code:"
        },
        {
          "prompt": "\"\"\"\nStep 4 of the Product Recommendation Engine (part 3) - ties everything\ntogether.\n\nRun it with:  python main.py\n\"\"\"\n\nimport pandas as pd\n\nfrom ab_test import tag_recommendations\nfrom batch import run_batch\nfrom catalog import load_catalog\nfrom load import load_customers\nfrom segment import segment_customers\n\n\ndef run():\n    print(\"Loading and segmenting customers...\")\n    customers_df = segment_customers(load_customers())\n    catalog = load_catalog()\n\n    print(\"Running the recommendation batch (this calls the AI once per customer)...\")\n    output_path = run_batch(customers_df, catalog)\n    print(f\"  Wrote {output_path}\")\n\n    rows_df = pd.read_csv(output_path)\n    tagged_rows = tag_recommendations(rows_df.to_dict(\"records\"))\n    pd.DataFrame(tagged_rows).to_csv(output_path, index=False)\n    print(f\"  Tagged all rows with campaign_id: {tagged_rows[0]['campaign_id'] if tagged_rows else '(none)'}\")\n\n    print(f\"\\nDone. See {output_path} for the full batch.\")\n    print(\"To push a recommendation to HubSpot, call push_recommendation_note() from\")\n    print(\"crm_push.py with your token, a contact_id, and one row from the CSV.\")\n\n\nif __name__ == \"__main__\":\n    run()\n",
          "verify": "Run `python main.py`. It should produce a tagged recommendations.csv where every row shares the same campaign_id. Then call push_recommendation_note() for one test contact with your HubSpot token and confirm the note appears on their timeline.",
          "instruction": "Create `main.py` with this code, then run the full pipeline end to end:"
        }
      ],
      "title": "Push to CRM and add an A/B tracker",
      "number": 4,
      "goFurther": "🛠️🛠️ After 30 days, join recommendations.csv against your orders data on customer_id + sku + date and compute a simple before/after conversion rate for recommended vs. non-recommended SKUs.",
      "description": "Write the top recommendation to each customer's CRM record, and tag batches so you can measure conversion lift later."
    }
  ],
  "outcomes": [
    "Load customer purchase history and segment customers by size and engagement, tested against exact boundary values",
    "Ground recommendations in a real product catalog, and drop any hallucinated SKU that isn't actually in it",
    "Enforce 'never recommend what they already own' in code, not just as a prompt request",
    "Filter low-confidence suggestions and fall back to real segment best-sellers instead of leaving a customer with nothing",
    "Push the top recommendation into a CRM as a note",
    "Tag a batch with a campaign ID for later conversion analysis"
  ],
  "portfolio": "Add this to your portfolio as \"AI Product Recommendation Engine\" -- a strong piece for sales ops, RevOps, or marketing tech roles. Show the segment breakdown, a sample customer's top-5 recommendations with reasoning, and how the hallucination guard keeps every SKU it suggests real and unowned.",
  "totalTime": "125 min",
  "buildCount": 4,
  "whatYouNeed": [
    "A free Gemini API key (aistudio.google.com/apikey) OR your own Claude/Anthropic API key (console.anthropic.com/settings/keys)",
    "Python 3.10+ installed",
    "A customers.csv (customer_id, past_purchases, industry, company_size) and a catalog.json of products",
    "Optional: a HubSpot account with a Service Key (Settings > Integrations > Service Keys) for the CRM push"
  ],
  "portfolioPrompt": "I just built an AI Product Recommendation Engine: it loads customer purchase history, segments customers by company size and engagement level, loads a product catalog, and uses an LLM (Gemini or Claude) to generate the top 5 personalized product recommendations per customer -- each with a confidence score and a plain-English reason -- with two code-level guards that drop any hallucinated SKU not in the catalog and any SKU the customer already owns. It filters out low-confidence suggestions with a real best-seller fallback, batch-processes the entire customer base into a CSV, pushes the top recommendation as a note into HubSpot contact records, and tags each batch with a campaign ID for A/B conversion tracking.\n\nHelp me write:\n1. A 3-4 sentence portfolio project description focused on the revenue/conversion angle\n2. A LinkedIn post (under 150 words) for a sales ops / RevOps audience\n3. Two resume bullet points that quantify potential upsell impact"
}$sess$::jsonb,
  null,
  null,
  $tsh$[
  {
    "fix": "Manually check size_segment() and engagement_segment()'s boundary values (49/50, 1/2, 4/5) against a handful of customers you know well before trusting the full batch output - segment.py's self-test already confirms the boundaries themselves are correct, so a wrong-looking segment usually means the input data (company_size or past_purchases) wasn't what you expected.",
    "issue": "Customer segments look wrong after grouping"
  },
  {
    "fix": "Add either AI_PROVIDER=gemini plus GEMINI_API_KEY=... (free, from aistudio.google.com/apikey), or AI_PROVIDER=claude plus ANTHROPIC_API_KEY=... (from console.anthropic.com/settings/keys) to your .env file, in the same folder as sdt_ai.py.",
    "issue": "AI request failed: No AI provider configured"
  },
  {
    "fix": "This shouldn't reach your output - recommend.py's validate_recommendations() already drops any SKU not in valid_skus(catalog) and prints which ones were dropped. If you're seeing a fake SKU anyway, confirm you're calling recommend_for_customer() (which validates) and not build_prompt()/parse_recommendations_reply() directly.",
    "issue": "Recommendations reference products that don't exist in your catalog"
  },
  {
    "fix": "Check that build_prompt() is actually receiving the full catalog and a distinct customer profile per call - if size_segment or past_purchases look identical across customers, the segmentation step upstream may not be running correctly. Increasing catalog variety or explicitly asking for segment-appropriate variety in the prompt can also help.",
    "issue": "Recommendations feel repetitive across different customers"
  },
  {
    "fix": "The prompt already asks explicitly for varied, meaningfully different confidence scores based on fit strength - if they're still uniform, the AI may be defaulting to a 'safe' middle score. Ask it to justify each score with its one-sentence reason (already required) and re-run; a model that has to explain the number tends to vary it more.",
    "issue": "Confidence scores are always the same number"
  },
  {
    "fix": "Check compute_bestsellers() actually found any purchase history for that customer's size_segment - if an entire segment has no past purchases at all, there's nothing to fall back to. In that case, consider seeding a manual best-seller list for segments with too little data.",
    "issue": "A customer ends up with zero rows in recommendations.csv"
  }
]$tsh$::jsonb,
  $res$[
  {
    "url": "https://pandas.pydata.org/docs/",
    "title": "pandas Documentation"
  },
  {
    "url": "https://developers.hubspot.com/docs/api/crm/notes",
    "title": "HubSpot Notes API"
  },
  {
    "url": "https://docs.anthropic.com/en/docs/build-with-claude/batch-processing",
    "title": "Anthropic Message Batches"
  }
]$res$::jsonb,
  'builder2',
  '2026-09-26 publish: shared sdt_ai.py updated (gemini-flash-latest with thinking turned down, readable errors for retired models and empty answers, Gemini key sent in a header), Python/venv setup steps, current HubSpot/Airtable/DataForSEO/ngrok/Railway/Buffer steps, code fixes. No credits: students use their own Gemini or Claude key.'
)
on conflict (course_id) do update set
  what_you_build = excluded.what_you_build, what_you_learn = excluded.what_you_learn, session = excluded.session,
  starter_code = excluded.starter_code, test_it_out = excluded.test_it_out, troubleshooting = excluded.troubleshooting,
  resources = excluded.resources, tier = excluded.tier, change_note = excluded.change_note, updated_at = now();
