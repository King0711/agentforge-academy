# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Uncompressed source video — placeholder for local preview only.
# GitHub rejects files over 100MB outright, and serving this raw size to
# every homepage visitor would be a real problem on its own even if it did
# fit. A compressed version needs to replace this before it's committed.
public/hero-video.mp4
